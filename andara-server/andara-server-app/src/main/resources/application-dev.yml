spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.andara: DEBUG
    org.springframework.kafka: DEBUG

# Note: For Docker deployment, set these environment variables:
# SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/andara
# SPRING_KAFKA_BOOTSTRAP_SERVERS=kafka:29092

